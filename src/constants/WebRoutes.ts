enum WebRoutes {
  HOME = '/',
  LOGIN = '/login',
  POSTS_ID = '/post/:postId',
  WRITER_POSTS = '/writer/posts',
  WRITER_PROJECTS = '/writer/projects',
  WRITER_DASHBOARD = '/writer/dashboard',
}

export default WebRoutes;
